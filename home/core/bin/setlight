#!/usr/bin/env bash

sudo systemctl stop colord && xiccd &

sleep 2 && profile_id=$(colormgr get-profiles | grep -B 3 "built-in" | grep "Object Path" | awk '{print $3}')

colormgr device-add-profile xrandr-eDP-1 $profile_id
